spring:
  application:
    name: basketservice

  data:
    mongodb:
      uri: mongodb://localhost:27017/basket-service
    redis:
      host: localhost
      port: 6379
      password: sa

  cache:
    redis:
      time-to-live: 60000
basketservice:
  client:
    platzi: https://api.escuelajs.co/api/v1
